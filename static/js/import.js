let raw='';const upload=document.querySelector('#upload'), preview=document.querySelector('#preview');upload?.addEventListener('submit',async e=>{e.preventDefault();const r=await fetch('/api/import/preview',{method:'POST',body:new FormData(upload)}),d=await r.json();if(!r.ok){document.querySelector('#error').textContent=d.error;return}raw=d.raw;preview.hidden=false;document.querySelector('#summary').textContent=`Messages imported: ${d.total} · Participants detected: ${d.participants.length}`;document.querySelector('#sample').textContent=d.messages.map(x=>`${x.sender}: ${x.message}`).join('\n');document.querySelector('#target').innerHTML=d.participants.map(x=>`<option>${x}</option>`).join('')});document.querySelector('#start')?.addEventListener('click',async()=>{const r=await fetch('/api/import/commit',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({raw,target:document.querySelector('#target').value})});if(r.ok)location='/';else document.querySelector('#error').textContent=(await r.json()).error});